{
  "name": "rainy",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "jquery": "^4.0.0",
    "jquery.ripples": "^0.6.3",
    "rainyday": "^0.0.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^5.0.0"
  }
}
