{
    "name": "Dev Environment",
    "postCreateCommand": "npm install && (cd backend && npm install)",
    "postStartCommand": "npm run dev & (cd backend && npm run dev) & sleep 4 && echo '' && echo ' [로컬 테스트 준비 완료!]' && echo ' Frontend: http://localhost:5173' && echo ' Backend: http://localhost:8787' && echo ''"
}